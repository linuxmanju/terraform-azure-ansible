---

- name: Wait for ssh as Azure takes time
  wait_for:
    port: 22
    host: '{{ (ansible_ssh_host|default(ansible_host))|default(inventory_hostname) }}'


- name: Debug
  debug: "{{hostvars[groups['app_hosts'][0]]['ansible_eth0']['ipv4']['address']}}"
